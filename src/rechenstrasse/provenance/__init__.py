"""What a run writes down so that somebody else can produce the same number.

Record 0007 fixes the five facts a run records and the byte identical replay
that goes with them. The provenance block in the output is issue #60. Nothing
here yet.
"""
